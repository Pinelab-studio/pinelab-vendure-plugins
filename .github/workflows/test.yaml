name: Deploy Vendure API and worker to prod and dev
on: ['pull_request']

jobs:
  test:
    name: Run tests for all packages
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: yarn
      - run: yarn test
