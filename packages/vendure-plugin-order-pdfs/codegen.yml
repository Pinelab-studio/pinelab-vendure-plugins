schema: 'src/**/*.graphql.ts'
generates:
  ./src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      avoidOptionals: false
      scalars:
        DateTime: Date
        ID: string | number
