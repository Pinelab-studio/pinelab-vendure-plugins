schema: 'src/api-v2/graphql-schema.ts'
generates:
  ./src/api-v2/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      enumsAsTypes: true
      avoidOptionals: false
      scalars:
        DateTime: Date
        ID: string | number
